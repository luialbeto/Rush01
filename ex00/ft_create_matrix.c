#include <stdlib.h>
#include <stdio.h>

int	**ft_create_matrix(int row, int col)
{
	int **matrix;
	int i;
	int j;

	i = 0;
	matrix = (int **) malloc(row * sizeof(int *));
	while(i < row)
	{
		j = 0;

	}
	return(matrix);
}
